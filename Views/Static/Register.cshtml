@{
    ViewData["Title"] = "Register";
}

<div class="bg-dark row justify-content-center"
    style="background-image: url(images/port-lights.jpg); background-size: cover;">
    <div class="col-sm-4" style="margin: 7rem 0;">
        <div class="card" style="background-color: #FFFFFFAA; backdrop-filter: blur(3px);">
            <div class="card-body">
                <h5 class="card-title">Register</h5>

                <form action="Register" method="POST">
                    <div class="mb-3">
                        <label for="emailInput" class="form-label">Email Address</label>
                        <input type="email" name="email" class="form-control" id="emailInput"
                            placeholder="name@example.com" />
                    </div>
                    <div class="mb-3">
                        <label for="passwordInput" class="form-label">Password</label>
                        <input type="password" name="password" class="form-control" id="passwordInput"
                            placeholder="Password" />
                    </div>
                    <div class="mb-3">
                        <label for="confirmPasswordInput" class="form-label">Confirm Password</label>
                        <input type="password" name="confirmPassword" class="form-control" id="confirmPasswordInput"
                            placeholder="Retype Password" />
                    </div>

                    <div class="card-text mb-3">
                        <p>By registering, you agree to the <a href="#">Terms & Conditions</a> and <a href="#">Privacy
                                policy</a>.</p>
                    </div>

                    <div class="text-center">
                        <button type="submit" class="btn btn-primary">Register</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>